
export enum Permissions {
    ADMIN = 'admin',
    READ_ONLY = 'read-only'
}

export enum loyaltyUser {
    GOLD_USER = 'Gold_User',
    SILVER_USER = 'Silver_User',
    BRONZE_USER = 'Bronze_User'
}